package Framework;

public class Counter {
	private int count, max;
	private float startX, startY, canDif;
	public Counter(int max) {
		this.max = max;
		this.count = 0;
		this.startX = 0;
		this.startY = 0;
	}
	/** ???????? 0???? ??? */
	public void counterInit() {
		this.count = 0;
	}
	/** @param canDif ????? ????????? ???????????? ????? ??????
	 * @param startX ?? ??? ????? ?? 
	 * @param startY ?? ??? ????? ??   */
	public void init(float canDif) {
		this.canDif = canDif;
		this.count = 0;
	}
	/** ??????? ???? + 1??? */
	public void update() {
		if(this.count <= this.max) this.count++;
	}
	/** ??????? ???? + 1??? ??????? ????????? count = 0??? ????? ??? ???????*/
	public void update(float x, float y) {
		update();
		if(Math.abs(startX - x) > canDif || Math.abs(startY - y) > canDif) {
			this.count = 0;
			this.startX = x;
			this.startY = y;
		}
	}
	/** @return ?????? ?????ð??? ????? ??? true?? ?????? ?????? ??????? */
	public boolean release() {
		if(this.count > this.max){ 
			this.count = 0;
			return true;
		}
		return false;
	}
}
